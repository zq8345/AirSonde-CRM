# 自动补邮箱：效果测量基线（冻结）

> **冻结时刻：2026-07-28T09:10Z**（提取器修复 `60ab289` 上线后、cron 尚未跑过任何一轮之前）
> 用途：明天用**同一口径**复测，算真实增量。**基线必须先冻结，否则"增量"是跟记忆比，不算数。**

## 生产数字（只读取自 `/api/health/sending` + D1）

| 指标 | 基线值 |
|---|---|
| 可发池（`status='approved'` 且有邮箱） | **27** |
| 卡住（`status='approved'` 但无邮箱） | **117** |
| 全库线索总数 | **503** |
| 全库无邮箱 | **265** |
| 其中有官网（技术上可补） | **265**（= 全部） |
| `find_email_enabled` | true |
| `find_email_last` | 空（**cron 还没补到过任何一个**） |

## 已上线的相关改动
- `ac5468c` 自动补邮箱进 cron（每轮 20 家、1.2s 礼貌间隔、只走免费路径、可关）
- `e1c9cce` 只读诊断端点 `find-email-diag`
- `1c62576` 提取器补三条路（原始 HTML 属性 / 畸形 `http://` 链接 / `data-cfemail` 解码）+ `valid()` 通用清洗

## 明天要看的三个数（同口径）
1. `find_email_last` —— 有没有真跑过、每轮补到几个
2. 可发池：27 → ?
3. 全库无邮箱：265 → ?

## ⚠️ 解读时必须记住的三条边界
1. **12 家抽样命中 8/12 ≠ 全库命中 67%**。那 12 家是「已批准且无邮箱」的前 12 条，不是随机样本；全库 265 家的构成不同。
2. **别用手点压测去"验证"**：反复压同一批站会触发 **429**，把自己的能力测低（7-28 就这么把 3/12 测成了 2/12）。
3. **剩下抓不到的一部分是现实不是 bug**：实测有站点 `mailto=0 / cfemail=0 / 页面无明文邮箱`，只有联系表单。那部分只能靠 Hunter（1 家 1 积分，归 Joe 定预算）或换渠道。
