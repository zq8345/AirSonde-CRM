# 三红修复复审（AU-CRM-2 后续）— 2026-09-02

**执行**：AirSonde-审计窗 · 只读复审，基线 `ca24bd6`（=origin/master，未推数 0 已核）
**结论**：**四件全部通过**。含一处对本窗上一轮建议的否定，成立且如实记录。

| # | 复审项 | 判定 | 证据 |
|---|---|---|---|
| ① | 归位钩子拆旗后 rescore 路径真执行 | ✅ | **可达性**：直读 `service.ts:296` 区——写分语句到钩子 UPDATE 之间已无任何条件分支（`scoreOnly` 仅剩草稿分支引用）。**内容正对照（本地 D1 实测）**：人造三形态执行钩子 SQL 原文——#901 approved/未放行/30 分 → **退回 analyzed** ✓；#902 approved/**human_approved=1**/30 分 → **不动** ✓；#903 **queued**/30 分 → **不动** ✓。⚠️ 含真实 AI 打分的全链路 E2E 本窗沙箱无钥匙跑不了，留执行窗试跑（可达性已由直读证死，风险余量小） |
| ② | 整点班撤发信、发送单一路径 | ✅ | `sendApprovedBatch` 全仓仅剩 2 调用方：手动端点(index.ts:1969)与 fastTick(4120)；整点 2.6 步改为「熔断评估」。**优于原建议**：`evaluateSendBreaker()` 下沉至 fastTick 发送分支之前（4085 < 4120）——伤口检查从每小时变为**每次开枪前**，我上轮只建议"每 N tick 一查" |
| ③ | analyzePending 原子认领 | ✅ | `analyzing_at` 列 + `UPDATE … WHERE id=? AND (analyzing_at IS NULL OR < now-10min)` + `changes!==1 → skip`（index.ts:3898）——照 send.ts:854 claim 同模式不另造；释放靠 status 迁移与 10 分钟过期，抓失败线索最多延迟 10 分钟重试，语义自洽 |
| ④ | 预算/锁不变式 + 闸反向自证 | ✅ | `TICK_BUDGET_MS=4min` 显式传参（4059），guard-cadence **双向**把守：正样本绿（`预算 240000 < 锁 300000 且 > 最大步需求 90000 ✓`）；本窗构造两个反向样本（scratchpad 拷贝改坏常量）——预算 6min>锁 ⇒ **红** ✓；预算 80s≤最大 has(90s) ⇒ **红** ✓。完整正反闭环 |

## 必须如实记的一条：本窗上轮建议有 bug，CRM 窗拒绝得对

上轮建议「tick budget 对齐 50s」。CRM 窗指出：发送闸是 `budget.has(50_000)` 而 `has()` 是**严格大于** ⇒ 总预算=50s 时恒假，**一封不发且无任何报错**——我的建议会静默杀死发信。其 4min（<锁 5min 且 >最大步需求 90s）+ 把不变式两头写进部署闸，是正确解。
⇒ 宪章补一条案例：**给别人开修复处方时，处方本身要过一遍"边界值代入"——严格不等号旁的"对齐"建议是静默死链的温床。**

*复审全程只读；本文件为唯一写入，提交权归 CRM 窗（按总工新规：审计报告默认入仓）。*
