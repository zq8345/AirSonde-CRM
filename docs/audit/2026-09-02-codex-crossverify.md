# Codex 深度审计报告逐条核验（AU-CRM-3）— 2026-09-02

**派单**：总工（Joe 亲自要求）· 任务=验证 Codex 22 条的真伪，非重新审计
**执行**：AirSonde-审计窗 · 全程只读（独立 worktree，未改代码/未部署/未动库/未发信）
**实审基线**：`6369e76` = origin/master（Codex 基线 `fd0028f` 之后有 3 提交：f763285 机器房两态 / a24fb2b 版本戳真源 / 6369e76 宪法铁律十——正是"已过时"判定的关键）
**裁定纪律**：每条给代码证据（文件:行号）或实测；宪章判据全程适用（尤其 5b 枚举调用方、"没查到≠不存在"）。

---

## 执行摘要

**22 条：坐实 19 · 坐实但定级夸大 2（两条 P0）· 已过时 1（P1-08）· 误报 0 · 未能核实 0。**

Codex 报告质量高：行号基本命中、无一条编造、自带边界声明（潜伏 vs 活跃分得清）。需要校正的只有两处：**两个 P0 的"事实全对、严重度按触发面需降为 P1"**（皆为 Joe 手点按钮的人工路径，非机器路径，且有边缘 Access + 前端确认框 + 压制双闸兜着）；**P1-08 已被 f763285 修掉**（Codex 基线时为真）。

| 裁定 | 条目 |
|---|---|
| 坐实 | P1-01 ~ P1-07、P1-09、P2-01 ~ P2-06、§3.1 ~ §3.5（共 19） |
| 坐实但定级夸大 | P0-01、P0-02（事实成立，独立定级 P1） |
| 已过时 | P1-08（f763285 修复，本窗代码实证） |
| 误报 / 未能核实 | 0 / 0（个别子项标"待验"，见逐条） |

---

## 逐条裁定表

### P0 两条

**P0-01 单条发送绕过 approved 与日限 —— 裁定：坐实（事实）＋夸大（定级）→ 独立定级 P1**
- ✅ 坐实：路由注释「（要求已批准 approved）」（index.ts:1950）与函数注释「要求 lead 已 approved」（send.ts:617）**都是说谎注释**——实际闸只有 key/邮箱/SUPPRESSED_STATUSES（send.ts:619-621），无 approved 要求、无日限（亲读）。
- ⚠️ 校正一：**"绕过日限"一半是明文设计**——send.ts:296「手动单条(sendLead)本就不过任何日限——豁免是结构性的」。Codex 当 bug 报，仓库当特性写。这属于"设计需 Joe 重新拍板"而非单纯缺陷。
- ⚠️ 校正二：触发面=后台鉴权（边缘 Access 302 实证）+ 前端确认框「发出不可撤」（index.html:3631）+ 压制双闸（状态闸+deliverEmail 持久名单）。**是 Joe 手点次数的问题，不是机器失控**——与执行窗判断一致，独立复核后同意。
- 独立定级 **P1**：说谎注释+服务端纵深缺失必须修（非 approved 该 409），但不构成"立即停用人工单发"级别。

**P0-02 暖跟进直发绕过跟进策略 —— 裁定：坐实（事实）＋夸大（定级）→ 独立定级 P1**
- ✅ 坐实：路由（index.ts:2038-2045）读任意 lead+自由正文→sendWarmFollowupNow（send.ts:685-691 只查 key/邮箱/SUPPRESSED/非空）→deliverEmail(followup)；followup 无幂等（deliverEmail 幂等仅 initial）——"可反复发"为真。
- ⚠️ 同 P0-01 的触发面校正：每次都要人点（index.html:3147-3150 有按钮禁用防双击，但服务端无幂等）。独立定级 **P1**，与 P0-01 合并修（统一发送 command）。

### P1 九条

**P1-01 并发非原子（A 锁/B 配额/C 幂等）—— 裁定：坐实（A 为残余型）**
- A：读后写锁（index.ts:4171-4174）TOCTOU 真；finally 无条件清锁（4247）"旧 owner 清新 owner"真。⚠️ 校正：Codex 基线已含 ca24bd6 的预算(4min)<锁(5min)不变式（其 §4 也承认 cadence guard 过），**主通道"跑过自己的锁"已堵**，残余=单个 await 悬挂超预算的病态场景+同分钟双触发，窗口窄。
- B：check-then-act 配额真（send.ts room 计算两处）；有界性：原子取批保证每 lead 至多一发，超发上限≤并发批的 take。
- C：**坐实且是三者中最实的**——schema.sql:95-99 emails 五个索引**全非 UNIQUE**（亲查）；手动 sendLead 不做原子认领（对比 fastTick 路径的 claim），手动+自动同 lead 并发可双发。
**P1-02 JWT 未接线 —— 坐实**：`grep accessjwt src/index.ts` = **0 命中**（亲测）；admin host 只查头存在（index.ts:250-253）；且 accessjwt.ts:27 `DEFAULT_ACCESS_AUD` 注释自认是 **wanew-crm 应用的 AUD**——接线那天若不配 ACCESS_AUD 会拿别家应用验票，这颗雷 Codex 点得准。缓解=边缘 Access 实测在岗（302 三信号）；属纵深缺失非即时洞。
**P1-03 孤儿回复 XSS —— 坐实**：esc() 恰好转义 `&<>"` 不转 `'`（index.html:5547 亲读）；onclick 单引号上下文拼 from_email（4120）；RFC 5321 local-part 合法含 `'`；from_email 外源持久化。P1 定级合理（需恶意来信+管理员打开）。修复方向（data-*+委托）对。
**P1-04 迟到事件覆盖 won —— 坐实**：webhook.ts:108-115 与 replies.ts:242-248 无条件 UPDATE（亲读），对照人工渠道回复的终态保护（分叉真）。
**P1-05 SSRF 面 —— 坐实（现环境影响有限）**：redirect:"follow" 两处（scrape.ts:239 / findemail.ts:272 亲读）、无私网过滤。Workers 出网环境+当前无 Service Binding 使可利用性低，Codex 自己也标了边界——事实对，修复宜列 backlog 非急修。
**P1-06 看板口径矛盾 —— 坐实（与本窗 AU-CRM-2 独立发现互证）**：本窗铁律五反扫已列同族清单（372/276、sendable 三处+同名异义），双源一致；3b28be8（C5-38 2/5）已修漏斗标注一角，其余在途（C5-38 3/5..5/5）。⚠️ 注意宪法定的"数据看板整页删除"若执行，本条大半自然消解——修口径前先对齐 roadmap。
**P1-07 dirty 构建不可重现 —— 坐实（历史事实）＋部分已过时**：stamp-build.js:22 只**标记** +dirty 不拒绝（亲读，无任何 dirty 拒绝闸）；a24fb2b 已把版本戳换成 CF Version ID 真源（戳诚实了，dirty 部署仍不被阻止）。当前生产是否 dirty：**待验**（Access 后；验证法=登录看版本戳）。本窗 AU-CRM-2 的 Y6（24 commit 已 deploy 未 push）为同族佐证。
**P1-08 机器房控制不可用 —— 已过时**：f763285（Codex 基线后）已修——openMachineRoom 现显式复用 openSettings 的 fetchCfg+fillSettingsFields（index.html:5384-5390 亲读，注释明写「⛔不为机器房写第二套加载逻辑」）。Codex 在其基线上判得对，现已修。行为级复验（打开机器房字段有真值）：**待验**（登录态）。
**P1-09 schema 非自包含 —— 坐实**：`grep "error TEXT\|is_auto" schema.sql` = **0 命中**（亲测），代码却读写两列（send.ts:29 / replies）；运行时 catch-DDL 自愈+首收竞态窗口如 Codex 述。

### P2 六条 —— 全部坐实

| 条 | 裁定要点 |
|---|---|
| P2-01 | 验签前全量读体真（index.ts:3052/3071 亲读）。细化：HMAC 本需原始体，**缺的是大小上限**而非读顺序——Codex 修复方向也写的这个，一致 |
| P2-02 | 浏览器直投真，但系**已登记的设计取舍**（authInbound 注释明写两路各判各的；Turnstile 在途注释 AU-CRM-1 亲见）。坐实=事实，风险共识=P2 |
| P2-03 | 静态 token 无事件幂等真；**潜伏**属实——LARK 键未配（生产 503 fail-closed 本窗 AU-CRM-1 实测过） |
| P2-04 | hono **4.12.28** 亲证（node_modules）；今日复跑 `npm audit --omit=dev`：**1 moderate（hono）**（通告条数 4 未逐条比对，量级一致） |
| P2-05 | 无 .github、无 test script 亲证。nuance：仓有部署闸（typecheck/cadence/design，且已 build.command 物理化）——缺的是**行为级**测试，Codex 表述与此一致 |
| P2-06 | compatibility_date=2025-07-01 亲证；其"维护债非漏洞"的自我定性准确 |

### §3 五条 —— 全部坐实

1. subreq isolate 级全局：真，且**仓库自知**（cron_subreq_last 的 meter 注释把同样限度写得明明白白——本窗 AU-CRM-1 读过原文）。
2. send.ts esc 三字符（send.ts:207 亲读 `/[&<>]/g`），website 进 href 无引号转义：真（管理员输入，窄面）。
3. svix-id 读了不存（webhook.ts:36，无持久化去重）：真。
4. isEmailSuppressed 查询失败 fail-open：真——**本窗 AU-CRM-1 独立发现过同条（⚪3）**，双源互证；双闸语境（状态闸兜底）使其非裸奔，fail-closed 化建议成立。
5. __pycache__ 未跟踪存在：亲证在。

---

## 真正该立刻修的（本窗独立排序，不照抄 P0 标签）

1. **P0-01+P0-02 合并修：单发/暖发收进统一发送 command + 删说谎注释**——虽是手动路径，但"服务端无闸+注释撒谎"正是这仓最忌讳的形态；顺带把"手动豁免日限"这个明文设计交 Joe 重新拍板（写它的时候还没有每分钟自动流水线，语境变了）。
2. **P1-03 XSS**——唯一"外部输入→管理员浏览器执行"链，修复成本低（data-*+事件委托），收益即时。
3. **P1-01C 邮件唯一约束**——唯一能造成"同一收件人收两封"的并发洞（手动+自动同 lead）；一条 UNIQUE 索引+冲突处理。
4. **P1-04 状态转移矩阵**——won 被迟到 bounce 降级是真金白银的数据损伤。
5. **P1-02 接线 JWT 时先清 DEFAULT_ACCESS_AUD 的 wanew 值**——接线本身可排后（边缘 Access 在岗），但那个错误默认值是接线日的地雷，现在改一行成本最低。

**已被修/夸大/无需再排**：P1-08 已修（f763285）；两个 P0 降级 P1（触发面）；P1-05 SSRF 现环境影响有限列 backlog；P2-02/§3.1/§3.4 为已登记取舍或双闸缓解；P1-07 戳已修真源、dirty 拒绝闸并入 CI 议题（P2-05）。

---

## 覆盖率

| 项 | 状态 |
|---|---|
| 实核 | 22/22 全部有代码证据或实测（涉及文件亲读：index.ts/send.ts/webhook.ts/replies.ts/scrape.ts/findemail.ts/accessjwt.ts/lark-app.ts/schema.sql/wrangler.jsonc/package-lock/index.html/stamp-build.js） |
| 待验（登录态，留总工/执行窗） | ①当前生产版本戳是否仍 +dirty ②机器房修复后的行为级复验（字段真值/取消不跨页） |
| 未逐条比对 | P2-04 的 4 条通告明细（今日复跑量级一致：1 moderate/hono）；P1-06 的生产四区块数字（Codex 登录态所见，本窗无会话，但代码侧口径分裂已独立证实） |
| Codex 行号漂移 | 其前端行号（3280/3617）与现行文件有小幅漂移（提交推进所致），指称的调用点真实存在（3297/3632 亲证）——不影响裁定 |

**对 Codex 报告的总体评价**：22 条 0 误报、0 编造，边界声明诚实（潜伏/活跃、当前缓解都写了）。它的系统性建议（唯一发送入口、原子预留、幂等键、状态机、可重现部署）与本窗 AU-CRM-1/2 的方向完全同向。主要校正=两个 P0 的触发面定级与一条已过时。

*本窗独立性声明：核验中三处与本窗既有发现互证（P1-06↔AU-CRM-2 铁律五清单、§3.4↔AU-CRM-1 ⚪3、P1-07↔AU-CRM-2 Y6），已标明；无一条照抄总工起点结论，P0 手动/自动边界为独立复核后同意。*
