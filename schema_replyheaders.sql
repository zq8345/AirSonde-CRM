-- 收回复：存原始头（**总工跑，我不执行**）
--
-- ⭐ 这一列存的是**观察**，不是**判断** —— 这个区别是它被批准的全部理由。
--
-- 背景：veritasvans 的自动回复机器人（"Thank you for contacting us! …"）被当成了"客户回你了"，
-- 把 #106 推成 `replied`、占着 Joe 最该看的那一格 —— **没有任何人看过那封信**。
--
-- 我原本的方案是加 `is_auto` 列 + 按 RFC 3834 头判。**总工拦住了，他是对的，用的还是我自己的原则**：
--   > "`is_auto` 是一个结论。**我们现在连证据在不在都不知道。**"
-- 我一边说着"我没验证过 Lark 转发时保没保留 `Auto-Submitted` 头"，
-- 一边要加一个**建立在那个头之上的判断字段** —— 那就是"记录证据 ≠ 报告结论"在**设计层**的版本，
-- 而今晚我们已经在三个地方犯过它了（reply_fail_last 被阈值挡着不记 / b0 原话被丢掉换成自编句子 /
-- "8 个退订零误判"只看了 8 个）。这会是第四个。
--
-- 所以：**先存头，什么都不判，行为一个字不变。** 等下一封自动回复自己撞上来，读真头，再定规则。
-- （veritasvans 一天来两封，很勤快。**让证据来找我们，不要我们去锤它。**）
--
-- ⚠️ 为什么存 headerLines 而不是我们"关心的那几个头"：
--   存"关心的那几个" = 又一次拿现在的结论去裁剪未来的证据。
--   万一 Lark 剥了 `Auto-Submitted` 却留了 `X-Autoreply`、或者留了个我们没想到的头，
--   截取过的证据就答不了那个问题了。**观察要存全，判断留给以后。**
--
-- ⚠️ ALTER TABLE ADD COLUMN 在 SQLite 不支持 IF NOT EXISTS，**重复执行会报 duplicate column name**。
--    只跑一次；报这个错说明已经加过了，可安全忽略。
ALTER TABLE replies ADD COLUMN raw_headers TEXT;

-- ---- 不加索引，故意的 ----
-- 这一列现在**没有任何查询会用它** —— 它是给人读的证据，不是给机器筛的字段。
-- 等真定了规则、真要按头筛，那时再加索引也不迟。现在加 = 为一个还不存在的查询付代价。

-- ---- 存量不回填，故意的 ----
-- 已入库的 3 条（Michael 那封 + veritasvans 两封）的原始头**我们没留**。
-- 要回填得重新连 IMAP 拉 UID 1,2,3 —— 那要动生产游标，而且不值得：**新的自动回复马上就来。**
-- NULL 在这里是诚实的：它表示"这封信入库时我们还没开始存头"，而不是"这封信没有头"。
--
-- 跑完自检：
-- SELECT COUNT(*) AS total, SUM(CASE WHEN raw_headers IS NOT NULL THEN 1 ELSE 0 END) AS has_hdr FROM replies;
-- 期望：total=3, has_hdr=0（刚加的列，存量不回填）。之后每收一封新回复，has_hdr +1。
