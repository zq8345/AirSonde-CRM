{
  "name": "airsonde-crm",
  "version": "0.1.0",
  "private": true,
  "description": "AirSonde AI 自动获客系统（fork 自 Wanew-CRM）- 后台 + D1 + CSV 导入",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "npm run guard:tsc && npm run guard:cadence && tsc --noEmit",
    "_comment_deploy": "⚠️ deploy 前置 typecheck 是**部署闸**，不是方便，别加 --ignore 绕过。原因：pool/imap 漏 import 时 boot 200 + 0 Uncaught 全绿（那段只在 scheduled() 里跑，HTTP 摸不到），只有类型检查在部署前抓得到。guard:tsc 再往前一层：先证明这道闸**存在** —— 一道只有 node_modules 装对了才存在的闸，就是在依赖某人记得 npm ci。",
    "deploy": "npm run typecheck && wrangler deploy",
    "db:init:local": "wrangler d1 execute airsonde_crm --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute airsonde_crm --remote --file=./schema.sql",
    "db:seed:local": "wrangler d1 execute airsonde_crm --local --file=./seed.sql",
    "guard:tsc": "node scripts/guard-typecheck.js",
    "guard:cadence": "node scripts/guard-cadence.js"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "postal-mime": "^2.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260717.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.110.0"
  }
}
